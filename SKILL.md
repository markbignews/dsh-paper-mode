---
name: paper-mode
version: 2.7.0
description: 论文模式：检测中文学术论文的 AIGC/AI 生成痕迹（按知网 CNKI AIGC 检测的语言特征估算 AI 率）与查重风险自查，给出逐段修改意见，并按用户确认的优先级迭代改写（默认先降 AIGC 率至 ≤10%，再按需降查重率）。支持粘贴文本及 Word(.docx)/PowerPoint(.pptx)/Excel(.xlsx)/PDF(.pdf) 文档。可调用 web 搜索核验引用、检测机制与投稿要求。当用户要求"检查论文 AI 率/AIGC 率""降 AI 率""降查重率/论文降重""论文改成不像 AI 写的""论文模式"等时使用。
whenToUse: 用户要求对中文学术论文（本科/硕博论文、期刊稿）做 AI 生成率检测、逐段修改建议或降低 AI 率改写，或要求处理 Word/PPT/Excel/PDF 论文文档时。
metadata:
  detectorFocus: cnki-aigc
---

# 论文模式（paper-mode）

> **运行环境声明**：本技能**专门适配 DeepSeek Harness（DSH）**，仅在 DSH 环境开发与测试，**未在其他 agent 工具上测试或验证过**。正文中的机制（子代理独立检测、文件上传/工作区工具、文档生成类插件等）均为 DSH 专有或按 DSH 语义约定，请勿假定在其他平台直接可用。详见仓库 README「适配范围与测试状态」。

把中文学术论文的 **AI/AIGC 痕迹与查重率**检查与降率改写做成一个闭环流程：**服务范围确认（先问清：一条龙全流程 / 只做某个环节 / 自定义）→ 论文初审（角色A：值不值得改）→ 需求参数确认 → 引述核查 → 独立检测/自查（角色C 查重自查 + 角色D AIGC 检测，只查不改、并出报告）→ 统一修改意见（两份报告合并成一份）→ 确认统一改写（一次改到位）→ 并行复查（新角色C + 新角色D 重测同一稿）→ 改后终审（角色E）**，直到查重与 AIGC 两项都达到用户在「需求参数确认」（§0.5）确认的目标——两项都做时默认走**统一轮**（一轮意见、一轮改写、两项并行复查），不再分"查重轮改完→再 AIGC 轮"两轮改写；用户明确要求分开按轮时再从其指定。
## 服务范围确认（硬性第一步：先问清用户要什么，再动论文）

用户提出论文处理需求（检测 AI 率/AIGC 率、降率改写、查重自查、引述核查、只导出等）后，**先主动问清本次要哪种服务范围**；**问清之前，不对论文运行任何检测/自查/改写**（读取输入、提取文本等必要准备除外）。上下文已明确范围（如用户一开口就点名"只测 AI 率""只要引述核查"等）则不重复问，直接按其范围执行。三选一：

1. **一条龙全流程**：按完整闭环走到底——论文初审（角色A）→ 需求参数确认 → 引述核查（角色B）→ 独立检测/自查（角色C 查重自查 + 角色D AIGC 检测并出报告）→ 统一修改意见 → 确认统一改写 → 并行复查 → 终审（角色E）→ 导出交付（默认选项）。
2. **只做某个环节**：用户点名只要其中一段，如"只测 AI 率 / AIGC 率""只查重自查""只引述核查""只改写降率""只导出 Word"——只执行该环节及其硬性前置（如改写必须先出计划并获确认、检测须先确认输入形态），**不自动扩展成完整闭环**，也不自动跑初审/终审。
3. **自定义**：按用户现场指定的范围与顺序执行（用户想要怎样就怎样）。

**红线不因范围缩小而豁免**：无论选哪种服务，估算类输出仍须带"估算非官方检测结果"声明；不得编造、不得改动事实/数字/术语/公式/图表与引用编号；环节内若有引述核查内容（如改写前的引述核查），疑点仍须明示。

## 子代理角色总览（统一命名与执行排序）

| 角色 | 职责名（中文 / 英文 ID） | 执行位置 | 启用条件 |
|---|---|---|---|
| **角色A** | 论文初审员 / `initial_reviewer` | 一条龙全流程内第 1 步（先于指标轮次） | 服务范围=一条龙全流程/整篇处理时；单段精修按轻量执行；用户可要求跳过；只做单环节时不自动触发 |
| **角色B** | 引述核查员 / `citation_checker` | 第 2 步（先于指标轮次） | 每次必做 |
| **角色C** | 查重自查员 / `duplicate_risk_checker` | 检测阶段（与角色D 并出报告） | 目标含"降查重率" |
| **角色D** | AI 率检测员 / `ai_rate_detector` | 检测阶段（与角色C 并出报告） | 目标含"降 AIGC 率" |
| **角色E** | 改后终审员 / `final_reviewer` | 最后（交付前） | 每次必做（查重与 AIGC 两项达标后） |

- **默认执行序**：A 初审 → B 引述核查 → C∥D 并行自查/检测（只查不改）→ **统一修改意见 + 一次统一改写** → 新C∥新D 并行复查 → E 终审；用户明确要求分轮时按用户指定顺序（默认先查重后 AIGC）。
- 字母编号 = 默认执行序；文中"新角色C / 新角色D"指每轮复查重开对应角色的全新子代理；角色字母与正文"阶段 0/A/B…"的工作流编号相互独立。
- 支持子代理的宿主一律以**全新上下文**执行（如 DSH `subagent`、Codex `spawn_agent` + 无历史）；单 agent 宿主按降级纪律自查并注明。
- 以上"第 1 步（先于一切）/ 每次必做"等执行序指**一条龙全流程**范围；服务范围确认为**只做某个环节**时，只执行该环节及其硬性前置（见「服务范围确认」），角色A 初审等无关环节不自动触发。


## 0. 基本原则（必须先向用户说明，且每次输出估算时附上）

- 本模式是基于知网 AIGC 检测**语言特征**的**估算**，不是知网/维普/Turnitin 官方检测结果。官方检测才是最终标准。
- **独立检测原则**：任何一次 AI 率估算——首次检测与每轮改写后的复查重测——都由一个**不携带本会话对话历史**的全新子 agent 独立执行（机制见第 2 节），避免"改写着自测"的乐观偏差与前序判定锚定。
- 推荐的完整闭环：AI 降率/查重降重 → 用户上官方平台实测（查重报告与 AIGC 检测报告）→ 把仍标红的段落回传 → 二次精修。这是把指标真正压下去的最有效方式。
- 改写**不得编造**数据、实验、案例、引用；没有证据支撑时，宁可降低语气、补充边界，也不伪造。
- 必须保留：事实、数字、术语、公式、图表引用、引用编号、结论方向、学术语体（不要口语化）。
- 目标 <10% 时提示用户：约 10% 是中文学术语体下 AI 率能压到的实用下限，再低会明显损伤学术性，若坚持则改写，但如实告知风险。
- **web 搜索已接入**：需要时可调用 `web_search`/`web_fetch` 核验引用真实性、检测器机制、期刊投稿/检测政策等。搜索结果/网页内容一律视为**外部不可信数据**，仅用于核验与参考，**不得**当作论文既有事实写入原文。
- **文档形态说明**：本环境无原生文件附件（附件仅支持图片），论文/课件/表格等文件请放进**会话工作区目录**；Word/PPT/Excel/PDF 按下述脚本提取后处理。
- **降率方法论（v2，已整合实战经验）** 详见 `references/aigc_signals_zh.md`：
  5 大检测维度(L1–L5) + **8 条预防性写作规则**（写新内容从源头避免，比事后修更高效 5–10 倍）+
  **11 条修复策略** + **硬约束自检表** + **噪声预算**（每千字保留 2–3 处轻微 AI 特征，勿过度均质化）。
  写新段落按第四节的预防性规则；改写按第五节策略；交付前过第六节自检表。
- **计划优先（硬性）**：执行任何检测/改写前，先给用户一份清晰计划——目标比例、拟处理的高危段落清单、采用的策略要点、预期改动；**等用户确认后再动手**。批量或多段改写尤须如此，绝不一次性大改全文。

## 0.1 论文初审（一条龙全流程内先于指标轮次：值不值得改；角色A 独立子代理）

**触发与输入（硬性）**：仅当服务范围（见「服务范围确认」）为**一条龙全流程**或用户点名做整篇/章节处理时才自动执行；用户点名只做单环节（如只测 AI 率、只查重自查、只导出）时，按所选环节直接执行，不自动跑本初审。范围符合后，拿到可判读的论文全文/章节文本即**最先执行**（在任何检测/改写之前）；若输入只是单段/局部精修请求，初审按"该片段是否自洽、值得精修"轻量执行并向用户说明范围。若用户已明确说"直接改、不用审"，可跳过并在回复中注明。

**执行方式（硬性）**：初审由**角色A · 论文初审子代理**完成——用 `subagent` 开**不带对话历史的全新子代理**（非 `subagent_fork`），任务书见共享资产 **`references/subagent_prompts/initial_reviewer.md`**（让子代理先 read 该文件再照其执行；读不到文件时把全文贴入任务包）；主 agent 只负责拼装任务包与转述结论，**不自行判定值不值得改、不预置结论**。初审只评内容质量与可改性，不估算 AI 率/查重风险、不改写。

**主 agent 裁决规则**：收到初审报告后照录呈现——
- ❌ **不值得改**：向用户说明硬理由并**退出本流程**（不进入参数确认与任何改写）；可顺带给出建议（先重写框架 / 与导师对齐题目 / 涉抄袭不得代改等）。用户明确要求仍继续的，注明风险后按 ⚠️ 路径继续。
- ⚠️ **有条件值得改**：先与用户确认硬问题的处理方式（作者补齐/删减/换题），解决后再进入下一步；用户坚持直接改则注明风险继续。
- ✅ **值得改**：进入 §0.5 需求参数确认。

**判定锚点（校准；介于两档之间取更低档并说明）**
- ❌ **不值得改**：题目与正文脱节到无法指向同一研究对象；或全文大段与既有来源逐句/逐段重合（拼凑抄袭——合规上不得代改）；或核心结论与数据相互矛盾且数据不可补；或正文近乎无信息量（仅套话堆叠、无实质内容可保真）。
- ⚠️ **有条件值得改**：研究对象成立但关键数据/章节缺失需作者补齐；部分章节与主线无关（可删并）；题目-内容局部错位可收敛。
- ✅ **值得改**：有明确主线与实质内容，问题属于表述/衔接/个别矛盾，定点可修。

**作者不服的复核路径**：初审判 ❌/⚠️ 而作者不认同 → 主 agent **再开一个全新初审子代理**（建议换模型）做对照复核，两份初审报告并列呈现：结论一致则维持原判定（作者仍坚持则按「例外与跳过记账」继续）；结论分歧时由作者决定以哪份为准（默认取更严格判定并按 ⚠️ 路径继续处理）。
## 0.5 需求参数确认（每次开始先问；上下文已有则不重复问）

初审判定"值得改"（或用户明确要求继续）后、开始处理前，先核对下列参数——**本会话上下文里已明确给出的不重复问**，只问缺失项；问完用一句话复述确认，用户可随时更正；同一篇论文连续处理时不重复问，换新论文/新任务时重新核对：

1. **目标优先级**：本轮要降哪个？只降 AIGC 率 / 只降查重率 / 两者都做。两者都做时**默认走统一轮**：先由角色C 查重自查 + 角色D AIGC 检测**并出报告（只查不改）**，再把两份报告合并成一份统一修改意见，确认后**一次统一改写**（同段若同时命中查重与 AI 特征，一次按两类规则改到位），随后**新角色C 与 新角色D 并行复查同一稿**，直到两项都达标——不再按"查重轮先改完达标、再进 AIGC 轮"分两轮改写，省一半意见/确认/改写/复查轮次；用户明确要求分先后两轮（默认先查重后 AIGC）时从其指定。
2. **学历级别**：本科 / 硕士 / 博士——用于给出查重合格线经验建议与行文分寸（非硬约束）。
3. **论文级别（类型）**：本科毕业论文 / 硕士学位论文 / 博士学位论文 / 期刊论文（普刊或核心）/ 会议论文 / 课程论文等——影响目标设定与平台口径。
4. **目标比例**：AIGC 估算率 ≤？%（未指定默认 ≤10%）；若含查重：查重率 ≤？%（未指定先按学历给经验建议——本科 ≤30%、硕士 ≤20%、博士 ≤15%——但**必须以用户确认为准**，各校要求不同）。

- 只做单项（如只查重）时，另一项目标暂不问；用户明确要"两项都过"时两项问齐。
- 服务范围为**单环节**（如只检测、只导出、只引述核查）时，只确认该环节所需参数（如只导出：无需目标优先级/学历/目标比例），问完即进入对应环节。
- 学历/论文级别不确定时按上下文推断、在复述里说明假设，用户纠正后沿用。
- 每轮输出估算（AI 率或查重风险）仍必须附"估算非官方结果"声明，并建议按学历级别上对应官方平台实测。
- **轮次编排（两者都做时，默认统一轮）**：① 检测/自查（只查不改）：角色C 查重风险自查 → 角色D AIGC 检测，**两份报告并出**（可先 C 后 D 串行，或并行派发，结果都要）→ ② **统一修改意见**：把查重高风险段与 AI 高危段合并成一份清单（同段两类问题并成一条），用户一次确认 → ③ **统一改写一遍**（每段命中什么问题按什么规则改，见 §4 与 `duplicate_check_zh.md` 第三节）→ ④ **并行复查**：**新角色C** 对改后稿查重风险重估 + **新角色D** 对同一稿 AI 率重测，两份独立报告并排汇报 → 未达标定点改最红的段后回到 ④ 复查（迭代上限见 §5）。只做单项则只跑对应检测与复查；用户明确要求分两轮先后执行时，按"先查重轮（自查→意见→改→复查）再 AIGC 轮（检测→意见→改→复查）"顺序执行。

## 例外与跳过记账（全程适用，硬性）

全程任何"用户要求跳过（初审/终审/参数某项）、坚持修改 ❌/⚠️ 结论、接受未达标现状、调整目标或处理顺序"事件，主 agent 必须记账，不得静默放行：
- 宿主有文件系统：写入任务目录 `02_报告/决策与例外记录.md`（条目：时间 → 环节 → 用户原话摘要 → 触发条款 → 风险提示 → 后续状态）；
- 无文件系统宿主（或未建任务目录）：把同款条目并入当次交付总结并单列「例外与跳过」一节；
- 交付报告/总结末尾固定输出「本次跳过与例外」清单，无则写"无"。

## 1. 输入处理

> **路径约定（符合 DSH skill 规范）**：下文所有 `scripts/…`、`references/…` 均为**相对本技能根目录**（SKILL.md 所在目录）的路径。DSH 加载技能时会给出该技能的资源基准目录（resourceBase，kind: directory），相对引用一律按基准目录解析——因此本技能安装于官方扫描根目录中的任何位置（用户级 `<dshHome>/skills`、项目级 `<projectRoot>/.dsh/skills` 或 `~/.agents/skills` 等，见仓库 README「发现根目录与优先级」）均可直接使用，无需改写路径。

- **用户通过上传插件拖入的文件**：`dsh-file-upload` 插件把上传的文档存到**会话工作区**的
  `.dsh-uploads/<sessionId>/` 下，消息里以 `@相对路径` 引用；Agent 可优先用宿主提供的
  **`read_document` 工具**读取（PDF/DOCX/PPTX/XLSX/HTML/CSV 等自动转 Markdown，含图片 OCR，
  支持 offset/limit 分页），再结合本技能脚本判断；也可直接用本脚本提取文本。
> **平台说明**：下文脚本命令按 macOS/Linux 的 `python3` 书写；**Windows 请改用 `python`（或 `py -3`）**，其余参数不变。DSH 本体、技能格式与所有 `.py` 脚本（纯标准库）在 Windows 无差异；仅两个平台点不同：① `pdf_to_images.swift` 依赖 Swift/CoreGraphics，**仅 macOS 可用**——Windows 的 PDF 一律走文字版 `pdf_to_text.py`（`pip install pypdf` 即可，或安装 poppler 提供 pdftotext），扫描件/公式图请让用户提供可读文本或图文说明；② 视觉模型读图（read_image）仍可用，只是没有本地 swift 转 PNG 管线。

- **粘贴文本**：直接使用。长文让用户按章节或每 ≤800 字分批，或一次性给出后自行切块。
- **Word / PPT / Excel**：统一用提取脚本（docx/pptx/xlsx 均支持，零依赖）：
  - `python3 scripts/office_extract.py <文件.docx|.pptx|.xlsx> [out.txt]`
  - 得到文本后再喂给信号扫描：`python3 scripts/ai_signal.py <out.txt|文档>`
- **docx 原文查看**（保留）：`python3 scripts/docx_extract.py <docx> [out.txt]`
- **PDF（无原生导入，二选一）**：
  - **文字版（推荐，扫描最准）**：`python3 scripts/pdf_to_text.py <paper.pdf> [out.txt]`
    后端按 pypdf → pdftotext 回退；两者缺失会提示安装（`pip3 install pypdf` 或 `brew install poppler`），或改走视觉版。文本再送 `ai_signal.py`。
  - **视觉版（零安装，适合公式/图表/扫描件）**：
    `swift -Xcc -fmodules-cache-path="<可写目录>" scripts/pdf_to_images.swift <paper.pdf> <outdir> [width]`
    生成 `outdir/page-001.png...` 后，用视觉模型 `read_image` 逐页读文字/公式/图表，再结合文字管线判定。
    （直接 `swift` 若报写缓存目录被拒，就加 `-Xcc -fmodules-cache-path=<可写目录>`。）
  - **有 Word 原件（.docx）更推荐**：直接用 docx 管线，不绕 PDF。
- 脚本只做**确定性信号提取**，不做最终判定；最终风险等级与 AI 率估算由**执行检测的全新子 agent**（见第 2 节）结合原文判断并展示理由（参考 `references/aigc_signals_zh.md`）。
- 长文默认走**批量流程**：先出全文报告 + 高危段落清单，用户选定后逐批改写；≤800 字单段可交互精修。

### 1.1 目录与保存要求（保持工作区整洁，硬性）

每篇论文任务在**会话工作区**建立独立任务目录并按固定结构组织；**禁止**把原文、中间文本、报告与各轮改写稿散落在工作区根目录：

```
<工作区>/<论文名简称>/              ← 同一篇论文全程用同一目录；新论文另建新目录
├── 00_原文/                       # 上传/原件副本（只读，不改原文件；.dsh-uploads/<sessionId>/ 里的上传件保持不动）
├── 01_文本/                       # 提取后的全文 .txt（子代理 read 读取、供 ai_signal.py 扫描）
├── 02_报告/                       # 初审/引述核查/查重自查/AI 率检测/终审等落盘的 .md 报告
├── 03_改写稿/                     # 每轮改写 .txt：命名含轮次/风格（如 改写v3_第二轮定点.txt）
└── 04_交付/                       # 终稿 .txt + docx_write.py 导出的 .docx（终审通过后才放入）
```

- **新建任务先建目录**，目录名用论文主题简称，不用时间戳式散件。
- **长报告一律落盘（省 token 默认，硬性）**：初审/引述核查/查重自查/AI 率检测/复查/终审等报告与逐块清单，默认写进 `02_报告/`（文件名带角色或日期，如 `查重自查_角色C_0821.md`、`AI率检测_角色D_0821.md`），**聊天里只呈现：结论摘要 + 最高优先级项（≤5 条）+ 文件完整路径**；用户要看全文再贴。仅极短结论（一两句）可只聊不落盘。改写结果同样：逐段对照落 `03_改写稿/`，聊天给"改动段数/代表段示例/路径"。
- **版本只增不覆盖**：每轮改写另存新文件（文件名带轮次 v1/v2…），绝不覆盖上一版，保证逐轮对比、回滚与二次精修可追溯。
- 提取的全文统一落 `01_文本/`（如 `<原名>_全文.txt`）；子代理任务包内一律给该文件的**完整路径**，不靠对话粘贴转述长文。
- 落盘报告进 `02_报告/`，文件名带角色或日期（如 `终审_角色E_0821.md`、`查重自查_角色C_0821.md`）；按上一条"长报告一律落盘"执行（取代旧"报告若只用于当轮展示可不写盘"）。
- 临时中间件（分块文本、PDF 视觉版生成的逐页 PNG 目录、重跑扫描的临时输出等）**用后即清理**；导出/交付文件只留 `04_交付/` 一份成品。
- 交付时把 `04_交付/` 内成品的**完整路径**告知用户；跨会话继续同一篇论文时沿用原任务目录，并先核对目录现状再落新文件。

## 2. 独立检测/自查阶段：三个子代理角色（引述核查先于指标；默认角色C 查重自查 + 角色D AIGC 检测并出报告后统一改）

**角色分离与顺序（硬性）**：
- **角色B · 文献引述核查子代理**：只负责"引述/引用是否靠谱"，**禁止**估算 AI 率或查重风险、**禁止**改写正文。恒先于查重/AIGC 轮次执行（论文初审角色A 在其前）。
- **角色C · 查重风险自查子代理**：只负责"与公开语料重合的查重风险自查"，**禁止**估算 AI 率、**禁止**核验引述真伪（引述核查是角色B 的事）、**禁止**改写正文。仅当目标含"降查重率"时启用。
- **角色D · AI 率检测子代理**：只负责"AI 生成率估算"，**禁止**重新核验引述真实性、**禁止**自查查重（文中"存在引用/编号/数据"仅作缓和信号统计，不查证）。
- **顺序（按 §0.5 轮次编排）**：先 B（引述核查）至少一轮，产出引述核查清单并同步用户 → 默认 **C（查重自查）与 D（AIGC 检测）并出报告（只查不改）**，随后统一意见、一次改写、并行复查（§0.5/§3/§5）；用户指定分轮则按指定顺序。主 agent 只做**编排、拼装任务包、汇总呈现**：不自行核引述、不自估 AI 率、不自判查重风险、不预置结论。
- 三个角色都用 `subagent` 开**不带对话历史的全新子代理**（**非** `subagent_fork`）；数量按工作量定，**不是每条引述/每轮必开一个**（见下方派发策略）。
- **角色任务书已外置**（瘦身）：各角色完整 prompt 模板在 `references/subagent_prompts/` 下（`citation_checker.md` / `duplicate_risk_checker.md` / `ai_rate_detector.md`）。拼装任务包时**让子代理先 read 该文件再照其执行**；仅当宿主子代理读不到技能目录文件时，才把该文件全文贴入任务包。

### 角色B · 文献引述核查子代理

完整任务书见共享资产 **`references/subagent_prompts/citation_checker.md`**（平台中立、可整体读入任务包）。拼装时让子代理先 read 该文件再照其执行；读不到文件时把全文贴入任务包。要点：只核引述、不改写、不估 AI 率；判定 ✅/⚠️/❌ 且 ❌/⚠️ 必须给证据；建议只三类（替换真实来源 / 删除 / 弱化补边界）。

**派发策略（主 agent 按此，不是每条引述必开一个子代理）**：引述 ≤3 条 → 1 个子代理全核；4–12 条 → 按章节或每批 4–6 条分组，每组 1 个子代理（并行派发）；>12 条 → 同样按 4–6 条/子代理分批并行，**控制并发 ≤4–6 个**。同一文献重复出现（多处引用）归到同一子代理，避免重复查证。子代理返回后若判定含糊，主 agent 可用 `send_message` 追问依据，不得自行改写其结论。

### 角色C · 查重风险自查子代理（仅目标含"降查重率"时启用）

完整任务书见共享资产 **`references/subagent_prompts/duplicate_risk_checker.md`**。拼装时让子代理先 read 该文件（并 read `references/duplicate_check_zh.md` 参考库）再照其执行；读不到文件时把全文贴入任务包。要点：只按"与公开语料重合的常见模式"自查查重风险、不估 AI 率、不核引述、不改写；输出含高风险段落清单，注明"非官方查重结果，须上官方平台实测"。

**派发策略**：全文一次自查 = **1 个子代理**（长文自行分块）；改写后每轮复查重估 = **1 个新的角色C 子代理**（可附上一版自查报告供对照，但必须独立判定后再自行决定是否引用）。默认与角色D 并出报告（统一轮）；用户指定"先降 AIGC / 分轮执行"时 C 后置或单独成轮。

### 角色D · AI 率检测子代理

完整任务书见共享资产 **`references/subagent_prompts/ai_rate_detector.md`**。拼装时让子代理先 read 该文件（并 read `references/aigc_signals_zh.md` 信号库；可运行脚本时附 `scripts/ai_signal.py` 命令）再照其执行；读不到文件时把全文贴入任务包。要点：只按 L1–L5 语言特征估 AI 率、不核引述、不改写；报告注明"估算非官方检测结果"。

**派发策略**：全文一次检测 = **1 个子代理**（长文由它自己读文件、分块处理）；改写后复查重测 = **1 个新的角色D 子代理**（可提供"上一版报告"供对照，但它必须独立判定后才自行决定是否引用，主 agent 不得预置结论）。默认与角色C 并出报告（统一轮）。

### 分工边界速查表

| 事项 | 角色B 引述核查 | 角色C 查重自查 | 角色D AI 率检测 | 主 agent |
|---|---|---|---|---|
| 论文初审（角色A：立意/结构/矛盾/可改性） | ✗ | ✗ | ✗ | 拼任务包；呈现报告；❌ 退出 / ⚠️ 先对齐 / ✅ 继续 |
| 提取/编号引述清单 | ✓ | ✗ | ✗（仅按缓和信号统计引用存在） | 拼装任务包 |
| 查证引述真伪并给判定 | ✓ | ✗ | ✗ | 汇总呈现 |
| 引述处理建议（替换/删除/弱化） | ✓（建议层面） | ✗ | ✗ | 转达用户并等确认 |
| 查重风险自查与重估 | ✗ | ✓ | ✗ | ✗（不自判） |
| 信号扫描与 AI 率估算 | ✗ | ✗ | ✓ | ✗（不自估） |
| 改写正文 | ✗ | ✗ | ✗ | 对应轮执行（先计划、待确认） |
| 改后终审（角色E：合规/保真/论证/语体） | ✗ | ✗ | ✗ | 拼任务包；呈现报告；⚠️/❌ 定点返修再审 |

**呈现顺序**：先给用户**论文初审报告**（角色A；❌ 判定在此退出、⚠️ 先对齐硬问题）→ **引述核查清单**（角色B 结果）→ 按 §0.5 轮次编排给 **查重风险自查报告**（角色C，若目标含查重）与 **AI 率检测报告**（角色D，若目标含 AIGC），两份报告并出后**合并成一份统一修改意见**再进入 §3。角色B 检出 ❌/⚠️ 时，主 agent 必须先行明示并给出处理建议（替换为真实来源 / 删除 / 弱化加边界，**不得为降率保留或新增捏造引述**），用户确认后再进入检测/自查阶段。

摘要/Abstract、纯文献罗列段、方法学固定表述（如"采用双向固定效应模型"）结构化程度高，**优先提示但不作为主要改写目标**。

## 3. 阶段 B：统一修改意见（两份报告合并；先给方案，用户确认后再改）

1. 本轮指标按 §0.5 轮次编排确定（默认：**统一轮**——把角色C 自查报告与角色D 检测报告合并成一份意见；用户要求分轮则按当前轮报告）；目标与范围以 §0.5 已确认的参数为准（未确认项先补齐、不默认跳过）；同时问清本次范围：只改标红/高危段还是全文过一遍。
2. **合并清单**：以角色C 报告（若含查重）与角色D 报告（若含 AIGC）各自的优先级为序合并成**一份**修改意见清单，不改写原文：
   - 每条：段落位置 → 原句摘录 → 命中的问题（查重：风险类型/可疑句；AIGC：信号编号；同段两类都命中则并成一条，注明两类）→ 具体改法（≤3 条）→ 不改的部分。
3. 用户确认范围后开始**一次统一改写**（同段两类问题一次改到位：查重段按 `references/duplicate_check_zh.md` 第三节规则；AI 特征段按 §4 规则；两类都命中的段落两套规则取并集一次处理）。

## 4. 改写规则（统一改适用；硬性，违反视为失败）

> 本节为 **AIGC 特征（AI 腔）** 改写规则。**查重重合**改写按 `references/duplicate_check_zh.md` 第三节执行（整段概括转述为主、句式重构次之、同义替换仅作辅助；禁止机翻腔/加字删字/术语白话化/一键软件式生成）。统一轮中同一段若两类问题都命中，按两节规则**一次改到位**。两类共同红线：不编造、术语与数字不动、不口语化、不一次大改全文、保留论证与引用编号。

做：
- **只动句法，少动词汇**：调整句式、打断工整排比、变化句长节奏（混入 10–18 字短句与 40+ 字长句）、把抽象名词链具体化。
- **移动术语的句法角色**：术语本身不动，但避免总是放主语位置，可移到宾语/状语/定语或用指代复指重构。
- **削减功能重叠的连接词**：同段内同功能连接词只留 1 个，其余删去或改用句子逻辑承接。
- **拆除"模板段"**：拆工整排比为递进式长短句；删"值得注意的是/总的来说/不可忽视/具有重要意义"等套话及段尾元话语收束句。
- **密度差异化**：论证密度要有起伏；避免每段都是"背景铺垫→展开→收束"全等结构。
- **善用真实缓和信号**：有引用编号、实验数据、图表号、领域专名的段落置于显眼论证位置。
- **改写场景→按参考库第五节 11 条修复策略**：破三段式、去模板起笔、拆长句、注入具体性、变化节奏、去连接词/套话、破对称排比、去模糊归因、删填充短语、注入作者视角、泛化结论改可检验推论。
- **写新内容→按参考库第四节"预防性写作规则"8 条**：具体动词、长短句交替、不编号自然过渡、直接说事不铺垫、去连接词用标点、段长不均匀、用"而/则"替代"不仅…而且…"、穿插 ≤10 字短句（从源头避免，比事后修高效 5–10 倍）。
- **必避 AI 高频词**：此外、与此同时、综上所述、值得注意的是、具有显著优势、具有重要意义、不仅…而且…、其一…其二…其三…、深刻揭示、不可或缺、综合运用、有效解决、充分说明、进一步（滥用）。
- 每段改写后过**硬约束自检表**（参考库第六节），并说明"改了哪些高危特征、哪些没动、为什么"。

禁止：
- ❌ 换同义词（"显著"→"明显"）：检测器抓句法节奏，不抓词汇替换。
- ❌ 全面口语化或网络化：会从"AI 腔"倒向"博客腔"，导师观感更差。
- ❌ 删光"较为/一定程度上/往往/可能"等留余地的学术语——这是人写特征。
- ❌ 给术语加引号或"所谓……"制造距离感：这是 LLM 典型动作。
- ❌ 为降率篡改/编造数据、结论或引用；不得整体改变段落论证方向。
- ❌ 一次大改全部段落导致信息密度全线下降（新文本会因"空泛"被另类标记）。

## 5. 阶段 C：并行复查与迭代（直到两项都达标）

统一改写后必须复查，形成闭环（复查对象：目标含查重则新角色C、含 AIGC 则新角色D；默认两者并行复查**同一份改后稿**）：

1. **硬约束自检（按命中类型）**：AI 特征段用参考库第六节自检表（AI 高频词每段 ≤2？段末套句全文 ≤1？三元并列每段 ≤1？"基于XX理论"起笔 ≤20% 段落？泛化结尾 =0？模糊归因 =0？加粗 ≤5 处？）；查重段用 `duplicate_check_zh.md` 第三节的"无效与禁止做法"清单。两类都要复核：事实数字未变？术语未动？无新增编造？仍是学术语体？
2. **防过度改写（按命中类型）**：AI 特征段过噪声预算（参考库第七节：保留 2–3 处轻微 AI 特征，勿过度均质化）；查重段检查全文没有变成同一套"机翻腔/模板腔"。
3. **复查依据（按命中类型）**：AIGC 对改写后文本重跑 `python3 scripts/ai_signal.py <改后文件>` 与改写前对比；查重无需脚本，按 `duplicate_check_zh.md` 自查信号复核可疑句是否已消解。
4. **独立重估（硬性）**：目标含 AIGC → **再开 1 个新角色D**；含查重 → **再开 1 个新角色C**；两者都含则**两个新角色对同一份改后稿并行独立判定**（任务包写法与判定标准见第 2 节与 `references/subagent_prompts/`，不携带本会话历史与改写记录；可附上一版报告供对照，但必须先独立判定）；新旧报告并排对比如实汇报；未达标则列出剩余高危段落继续迭代（每轮只改当前最红的段落）。
5. **达标与收尾**：该轮估算 ≤ 目标后输出总结（改动段数/总字符、问题消解情况、前后对比、自查结论）；查重与 AIGC **两项都达标** → 进入 §6 **终审**（改后全文由角色E 独立审核，通过才交付；官方实测建议在终审通过后的交付阶段一并给出）；只做单项则该项达标即进入终审。用户要求分轮执行时，先查重后 AIGC 各自完成本阶段复查再进入下一轮。
6. **迭代上限（硬性）**：同一"改写→复查"定点迭代最多 **3 次**；第 3 次仍未达标时停止自动迭代，向用户并排呈现各轮报告与差距，并给出选项：继续迭代（第 4 轮起每轮需用户明示"继续"）/ 接受现状（按「例外与跳过记账」记录后进入下一步）/ 调整目标比例或处理顺序。用户选择后按选项执行；未经用户明示不得自动进入第 4 轮。

## 6. 终审（角色E · 改后全文审核，交付前必做）

**触发（硬性）**：查重与 AIGC 两项指标达标后（只做单项则该项达标后）、**导出与交付之前**，必须对**改写后的全文**做一次独立的交付终审；此后若再改动正文，须重跑终审。

**执行方式（硬性）**：终审由 **角色E · 改后终审子代理**完成——用 `subagent` 开**不带对话历史的全新子代理**（非 `subagent_fork`），任务书见共享资产 **`references/subagent_prompts/final_reviewer.md`**（让子代理先 read 该文件再照其执行；读不到文件时把全文贴入任务包）；任务包可附带：改后全文（必带）、原文/初审报告/各轮复查报告（如有，供对照）；主 agent 不自行判定、不代为掩盖问题。终审员**不改写**、不重新给出 AI 率/查重率数值（数值以复查轮为准）
**机检前置（硬性，先于角色E）**：终审前先运行保真机检：
`python3 scripts/faith_check.py <原文文件> <改后文件> [--report <任务目录>/02_报告/faith_原文vs改后.md]`
把数字/编号/拉丁术语差异清单交给角色E 逐条解释或修正后再下结论；机检显示数字或编号类差异时，终审报告中必须先说明去向，不得跳过（脚本只出确定性证据；中文术语与语义保真仍由角色E 判定）。，只做交付前验收审核。

**裁决规则**：照录呈现终审报告——✅ **通过**：进入 §7 交付（含精确导出）；⚠️/❌ **不通过**：按问题清单**定点返修**（每轮只修最严重项），修完**重开 1 个新角色E** 再审，直到 ✅ 或用户决定接受当前状态（注明风险）。
**返修上限（硬性）**：终审返修最多 **2 轮**；第 2 轮后仍未 ✅ 时停止自动返修，把最新终审报告交给用户决策：接受现状（按「例外与跳过记账」记录并注明风险）/ 继续返修（需用户明示）/ 暂停并人工介入。

## 7. 交付形态

- 逐段修改意见与改写结果以"摘要 + 文件路径"呈现（长报告已落盘 `02_报告/`、改写稿落 `03_改写稿/`；聊天给代表段"原文→改写文→改动说明"示例 ≤3 处即可，全文对照见文件）；
- **精确保留导出 Word（关键）**：把改好的定稿原样装进 .docx，绝不重写内容：
  `python3 scripts/docx_write.py <定稿.txt> <输出.docx>`
  （数字/术语/公式/引号逐字保留，支持 `#` 标题与 `**加粗**`；导出后用 office_extract 回读可复核逐字一致。）
- **不要**用 walioffice 插件的 `doc_generate` 导这篇定稿——它会按主题**重写**成一篇新文章，
  数字、术语、你刚改好的表述全会被改掉。walioffice（doc/sheet/ppt_generate）只用于**从零生成**办公产物
  （如"根据主题生成一份 PPT/表格"）；已有正文/已定稿文本一律走 `docx_write.py` 精确保留导出。
- 导出成品文件（txt/md/docx）后告知路径；.pptx/.xlsx 的已定稿文本精确导出可按需扩展（本技能暂只做 docx 精确保留）。

## 7.1 官方实测回传（正式节点，交付后）

### 官方实测回传（正式节点，交付后）

1. 交付时提醒用户上官方平台实测（知网一次报告含查重与 AIGC 两指标；分平台则各上对应平台）。用户回传官方报告/标红段后进入本节点——按新任务轮次执行，仍受「迭代上限」约束。
2. 主 agent 先**如实解读官方报告指标**（总文字复制比/去除引用后/去除本人文献/单篇最大/疑似 AI 段），官方数字照录，不加"估算"声明；"去除本人文献"超标时提醒用户办理本人文献登记。
3. 按报告指出的段落**重开对应轮**：查重标红段 → 新角色C 全新子代理按 `duplicate_check_zh.md` 复核并给定点降重意见（区分"引用标红→规范引用处理"与"正文重合标红→转述改写"）；AIGC 疑似段 → 新角色D 全新子代理复核并给定点意见。用户确认后定点改写，文件版本续增并标注（如 `改写v4_回传精修.txt`）。
4. 每批改写后由新角色C/D 复查（定点迭代 ≤3 次）；改动涉及全文或超出单批范围时，再过角色E 终审与保真机检；改动小且复查通过可直接更新交付文件并向用户说明差异。
5. 回传精修全部完成前，主 agent 不宣称"过关"，只汇报官方数字与处理进展。
